const names = ["Brooklyn", "Queens", "Bronx", "Manhattan"];
console.log(names[1]);
/*console.log(names.length);

for (const el of names) {
  console.log(el);
}
names.push("julie");
const julieIndex = names.findIndex((el) => el === "julie");
